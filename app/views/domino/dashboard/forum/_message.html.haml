%div.message-wrapper
  - if current_user.my_message?(m.author_id)
    %div{:class=>'author'}
      = m.author.name
  - else
    %div{:class=>'non-author'}
      = m.author.name
  %div{:class=>'content'}
    = m.message
  %div#chat-time
    = m.created_at.strftime('%H:%M')